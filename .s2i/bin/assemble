#!/bin/bash

set -eo pipefail

touch somefile && echo "Touched some other file"

pip install --upgrade pip setuptools wheel
pip install -r requirements.txt
python manage.py collectstatic --noinput

# /usr/libexec/s2i/assemble
